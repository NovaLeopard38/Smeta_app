"""
Leads router stub.
Endpoints will be migrated from app.py incrementally.

Endpoints to migrate:
- POST /leads
- POST /leads/quote
- GET /leads/me/history
- GET /leads/quote/restore
- GET /admin/leads
- GET /admin/leads/{lead_id}/quotes
"""
# TODO: Move endpoints from app.py as next refactoring step
