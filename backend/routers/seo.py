"""
SEO & site settings router stub.
Endpoints will be migrated from app.py incrementally.

Endpoints to migrate:
- GET /seo/pages
- POST /seo/pages
- DELETE /seo/pages/{slug}
- GET /sitemap.xml
- GET /robots.txt
- GET /site/settings
- POST /site/settings
"""
# TODO: Move endpoints from app.py as next refactoring step
