"""
Voice router stub (Tinkoff VoiceKit integration).
Endpoints will be migrated from app.py incrementally.

Endpoints to migrate:
- POST /voice/stt
- POST /voice/tts
- POST /voice/dialog
- POST /voice/call/start
- POST /voice/call/turn
- POST /voice/call/end
- GET /voice/debug/jwt
- GET /admin/voice/calls
- GET /admin/voice/calls/{call_id}
- PATCH /admin/voice/calls/{call_id}/status
"""
# TODO: Move endpoints from app.py as next refactoring step
